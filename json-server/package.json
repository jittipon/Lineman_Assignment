{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 9000",
    "english": "json-server --watch db-en.json --port 8000"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
